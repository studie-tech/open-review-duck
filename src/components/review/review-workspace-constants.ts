/** Lines added above or below the focus window when the reviewer pages in context. */
export const CONTEXT_PAGE_LINES = 20;

/** Initial number of review paths rendered in each sidebar section. */
export const INITIAL_PATH_ITEMS = 10;

/** Additional review paths rendered when a sidebar section is expanded. */
export const PATH_PAGE_SIZE = 20;

/** Maximum age of provider conversation data before a background refresh. */
export const PROVIDER_CONVERSATION_REFRESH_MS = 45_000;
